const { addByUsername } = require('./addByUsername');
const { addByDevice } = require('./addByDevice');
const { list } = require('./list');

module.exports = {
  addByUsername,
  addByDevice,
  list,
};
